[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "efesto"
version = "1.0.6"
description = "Efesto: HMM-based annotation of metal cycling and resistance genes in genomes and metagenomes"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.8"

# Pure Python standard library only — external tools (hmmsearch, prodigal,
# samtools) are installed via conda from environment.yml
dependencies = []

keywords = [
    "bioinformatics", "metagenomics", "iron cycling",
    "metal resistance", "HMM", "annotation"
]

classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Intended Audience :: Science/Research",
]

[project.scripts]
efesto = "efesto.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
